pluginManagement {
    includeBuild('../../build-logic')
    repositories {
        maven { url = 'https://maven.fabricmc.net/' }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'cbbg-fabric-modern'
include 'core'
project(':core').projectDir = file('../../core')
